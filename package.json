{
  "name": "typed-phoenix-live-view-hook",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/phoenix": "^1.5.6",
    "@types/phoenix_live_view": "^0.18.0",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
